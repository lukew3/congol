{
  "name": "congol",
  "version": "1.0.0",
  "description": "Multiplayer Conway's Game Of Life",
  "main": "index.js",
  "scripts": {
    "build": "pug pages -o ./ && browserify src/main.js -o src/bundle.js",
    "deploy": "gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-todir": "pug pages -o ./build/ && browserify ./src/main.js -o ./build/src/bundle.js && cp -r img build/img && cp -r styles build/styles && cp CNAME build/CNAME"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lukew3/congol.git"
  },
  "author": "Luke Weiler",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/lukew3/congol/issues"
  },
  "homepage": "https://github.com/lukew3/congol#readme",
  "dependencies": {},
  "devDependencies": {
    "browserify": "^17.0.0",
    "gh-pages": "^3.2.3",
    "pug": "^3.0.2"
  }
}
